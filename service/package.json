{
  "name": "service",
  "module": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "bun run src/index.js",
    "build": "bun build src/index.js --compile --outfile ../web/server-bin",
    "build:desktop": "bun build src/index.js --compile --outfile ../web/server-bin --external pg --external better-sqlite3 --external mysql --external mysql2 --external oracledb --external tedious --external pg-query-stream --external cockroachdb --external mssql --external sqlite3",
    "knex": "knex"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dayjs": "^1.11.19",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "socket.io": "^4.8.1",
    "sqlite3": "^5.1.7",
    "uuid": "^9.0.1"
  }
}
